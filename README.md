# sentinel-test

Deliberately vulnerable repository used to prove the Sentinel gate works.

Everything here is planted on purpose: leaked credentials, injection flaws,
vulnerable dependencies. Every value is randomly generated and valid nowhere.

If a scan of this repository comes back clean, the gate is broken.
